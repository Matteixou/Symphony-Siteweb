<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Mon E‑Shop{% endblock %}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🛍️</text></svg>">

        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

        {% block stylesheets %}
        <style>
            :root {
                --brand-accent: #0d6efd;
            }
            body {
                background-color: #ffffff;
                color: #111111;
                font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            }
            .top-bar {
                font-size: 0.8rem;
                letter-spacing: .05em;
                text-transform: uppercase;
            }
            .hero-title {
                letter-spacing: .08em;
            }
            main {
                min-height: 70vh;
            }
            .footer-title {
                text-transform: uppercase;
                font-size: 0.8rem;
                letter-spacing: .12em;
            }
            a {
                text-decoration: none;
            }
            a:hover {
                text-decoration: underline;
            }
        </style>
        {% endblock %}
    </head>
    <body>
        {# barre fine en haut #}
        <div class="top-bar bg-dark text-light py-1 d-none d-md-block">
            <div class="container d-flex justify-content-between align-items-center">
                <div>
                    Livraison offerte dès 80 € • Retours sous 30 jours
                </div>
                <div>
                    <a href="{{ path('app_contact') }}" class="text-decoration-none text-light me-3">Aide & Contact</a>
                    {% if app.user %}
                        <span class="text-muted">Connecté : {{ app.user.userIdentifier }}</span>
                    {% endif %}
                </div>
            </div>
        </div>

        {# header principal #}
        <header class="border-bottom bg-white">
            <div class="container py-3">
                <div class="d-flex flex-wrap align-items-center justify-content-between gap-3">
                    <a href="{{ path('app_home') }}" class="d-flex align-items-center text-dark">
                        <span class="fs-3 fw-bold me-2">ME</span>
                        <span class="fw-semibold text-uppercase">Mon E‑Shop</span>
                    </a>

                    <form class="flex-grow-1 mx-md-4" method="get" action="#">
                        <div class="input-group">
                            <input type="text" class="form-control border-end-0" placeholder="Rechercher un produit…">
                            <button class="btn btn-outline-secondary border-start-0" type="submit">Search</button>
                        </div>
                    </form>

                    <div class="d-flex align-items-center gap-3">
                        <a href="{{ path('app_cart') }}" class="btn btn-outline-dark position-relative">
                            🛒
                            <span class="d-none d-lg-inline ms-1">Panier</span>
                        </a>
                        {% if app.user %}
                            <a href="{{ path('app_profile') }}" class="text-dark small text-uppercase fw-semibold">
                                Mon compte
                            </a>
                            <a href="{{ path('app_logout') }}" class="text-danger small text-uppercase fw-semibold">
                                Déconnexion
                            </a>
                        {% else %}
                            <a href="{{ path('app_login') }}" class="text-dark small text-uppercase fw-semibold">
                                Connexion
                            </a>
                            <a href="{{ path('app_register') }}" class="btn btn-dark btn-sm text-uppercase fw-semibold">
                                S’inscrire
                            </a>
                        {% endif %}
                    </div>
                </div>
            </div>

            <nav class="navbar navbar-expand-lg navbar-light border-top">
                <div class="container">
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                    <div class="collapse navbar-collapse" id="mainNavbar">
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0 text-uppercase small fw-semibold">
                            <li class="nav-item">
                                <a class="nav-link{% if app.request.get('_route') starts with 'app_home' %} active{% endif %}" href="{{ path('app_home') }}">Accueil</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_product_index') }}">Boutique</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_contact') }}">Contact</a>
                            </li>
                            {# lien admin #}
                            <li class="nav-item">
                                <a class="nav-link text-muted" href="{{ path('app_product_index') }}">Admin produits</a>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
        </header>

        <main class="py-4">
            <div class="container">
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                            {{ message }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    {% endfor %}
                {% endfor %}

                {% block body %}{% endblock %}
            </div>
        </main>

        <footer class="bg-black text-light pt-5 pb-3 mt-5">
            <div class="container">
                <div class="row g-4">
                    <div class="col-md-4">
                        <h5 class="mb-2 text-uppercase">Mon E‑Shop</h5>
                        <p class="text-muted small">
                            Une sélection de pièces modernes pour un style quotidien performant.
                        </p>
                    </div>

                    <div class="col-md-4">
                        <div class="footer-title text-muted">Newsletter</div>
                        <p class="text-muted small">
                            Reçois en avant‑première nos drops et offres exclusives.
                        </p>
                        <form class="d-flex gap-2">
                            <input type="email" class="form-control form-control-sm" placeholder="Ton email">
                            <button class="btn btn-sm btn-light text-dark" type="submit">OK</button>
                        </form>
                        <div class="mt-3">
                            <div class="footer-title text-muted">Suivez‑nous</div>
                            <div class="d-flex gap-2">
                                <a href="#" class="btn btn-outline-light btn-sm rounded-circle">f</a>
                                <a href="#" class="btn btn-outline-light btn-sm rounded-circle">ig</a>
                                <a href="#" class="btn btn-outline-light btn-sm rounded-circle">x</a>
                            </div>
                        </div>
                    </div>

                    <div class="col-md-4">
                        <div class="row">
                            <div class="col-6">
                                <div class="footer-title text-muted">Boutique</div>
                                <ul class="list-unstyled small">
                                    <li><a href="{{ path('app_home') }}" class="text-light">Nouveautés</a></li>
                                    <li><a href="{{ path('app_product_index') }}" class="text-light">Tous les produits</a></li>
                                    <li><a href="{{ path('app_cart') }}" class="text-light">Panier</a></li>
                                </ul>
                            </div>
                            <div class="col-6">
                                <div class="footer-title text-muted">Légal</div>
                                <ul class="list-unstyled small">
                                    <li><a href="#" class="text-light">Mentions légales</a></li>
                                    <li><a href="#" class="text-light">CGV</a></li>
                                    <li><a href="#" class="text-light">Confidentialité</a></li>
                                </ul>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="text-center mt-4 pt-3 border-top border-secondary small text-muted">
                    &copy; {{ "now"|date("Y") }} Mon E‑Shop. Tous droits réservés.
                </div>
            </div>
        </footer>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
